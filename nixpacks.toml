# NIXPACKS build configuration
[phases.setup]
nixPkgs = ["nodejs_22", "bash"]

[phases.install]
cmds = ["npm ci --no-audit --no-fund --prefer-offline || npm install --no-audit --no-fund"]

[start]
cmd = "bash start.sh"