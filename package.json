{
  "name": "openclaw-cloud",
  "version": "1.0.0",
  "scripts": {
    "start": "openclaw gateway --port ${PORT:-8080} --bind lan"
  },
  "dependencies": {
    "openclaw": "latest"
  },
  "engines": {
    "node": ">=22"
  }
}